# QA Execution Report — FLOW-V1-01 — Family-A flow #1 (Search→FareConfirm→Book→Retrieve)

**Environment:** staging &middot; https://ndc-integration-stg-ne-3.azurewebsites.net/
**Supplier:** TravelportNdc (Credentials Selector: UAE)
**Started:** 2026-08-23 16:40:37 UTC
**Status:** INTERRUPTED — after FLOW-V1-01 (1/1)

## Summary

- Test cases logged: 1
- Passed: 0
- Failed: 1
- Blocked: 0

## Test case results

### FLOW-V1-01 — Verify FareConfirm-based Family-A flow #1 (SR-FC-BK-RT) completes with the expected end-state

- Endpoint Name: Flow #1 (Search &rarr; FareConfirm &rarr; Book &rarr; Retrieve)
- Request: 4-hop V1 chain, TravelportNdc/UAE, CAI&rarr;JED 2026-09-15, 1 ADT + 1 CHD + 1 INF
- Response: Search 200 (67 offers) / FareConfirm 200 (2,290 AED) / Book 409 `NDC-INT-TEC-A0002` / Retrieve not executed
- Expected (§2.1, §2.2, §2.9, §2.11, §6.3-Family-A): Retrieve shows bookingStatus=booked; selectedOffer.offerId and priceDetails.totalAmount match between Book and the follow-up Retrieve; price reconciliation holds at Search, FareConfirm and Book
- Actual: Flow did not reach its end-state — Book was rejected with HTTP 409 because the supplier (Travelport/QR) exhausted its infant seat quota for this specific flight/date, after already allocating an internal hold (GDS PNR `HMZQ50`). No `pnr`/`bookingToken` was returned, so Retrieve could not run.
- Status: **FAIL**
- Evidence: Book response — `{Status:409, ErrorCode:'NDC-INT-TEC-A0002', ErrorMessage:'A technical error occurred in NDC integration.', Context:[{Name:'TERMINAL', Value:"Booking is still in the hold stage. No ticket has been issued yet. PNR: N/A. GDS PNR: HMZQ50., INFANT QUOTA FOR FLIGHT HAS BEEN EXCEEDED. REBOOK WITH ALTERNATE FLIGHT (AIRLINE QR)"}]}`. The response body also contained a raw `OriginalSupplierResponse` field — redacted from this report per the skill's hard safety rule (SPEC.md §7.14 pattern).

Hops (4):
  1. Search — Status: **FAIL** — Response: 200 in 8.3s, 67 offers, offer[0] 2,290 AED, canBeHeld=true, haveBundles=false — Expected (§2.1): non-empty offers[], per-offer tax reconciliation. Actual: reconciliation fails — offer[0]'s `taxesAndFees[]` is empty for all 3 passenger types while `paxTotalTaxAmount` is non-zero (ADT/CHD 790, INF 370); `paxTotalAmount` absent — same pattern as the 2026-08-05 run (BUG-02/BUG-03).
  2. FareConfirm — Status: **PASS** — Response: 200 in 2.1s, 2,290 AED unchanged, canBeHeld=true, taxesAndFees fully itemized and reconciling for all 3 passenger types — Expected (§2.2): 200, current pricing, reconciliation holds, no drift vs Search. Actual: matches expected exactly.
  3. Book — Status: **FAIL** — Response: 409, `ErrorCode=NDC-INT-TEC-A0002`, hold created (GDS PNR `HMZQ50`) then rejected for infant quota — Expected (§2.9): 200, non-empty pnr/bookingToken, price reconciliation, voidCapability returned. Actual: rejected — live capacity constraint, not seen when this same route/date/passenger-mix booked successfully on 2026-08-05.
  4. RetrieveBooking — Status: **BLOCKED** — Not attempted (no pnr/gdsPnr/bookingToken from hop 3) — Expected (§2.11/§6.3): 200 and bookingStatus reflecting the Booked lifecycle stage. Actual: cannot execute.

## Findings

- **Unknown-resolution** — Book can reject with HTTP 409 `ErrorCode=NDC-INT-TEC-A0002` after the supplier has already allocated an internal hold (GDS PNR), when a per-passenger-type inventory quota (observed: infant) is exhausted on that specific flight/date. Confirms the previously-documented-but-unconfirmed race class at §2.9 Common Errors ("inventory sold out between FareConfirm/AddPassenger and Book") with a concrete, evidenced error shape. (§2.9, FLOW-V1-01)
- **Defect** — Book's error response body includes a raw, verbatim `OriginalSupplierResponse` blob (full supplier XML/JSON) even though Book has no `isdebug` field at all — the credential/internal-detail exposure risk documented at §7.14 for Search's `isdebug=true` is not gated by any flag on Book's failure path. Any caller who triggers a Book-side supplier rejection receives the full raw upstream supplier response unconditionally. (§7.14, FLOW-V1-01)

## Proposed SPEC.md updates

- **§2.9 Common Errors — pending** — Replace: `"Common Errors: stale/expired offer reference; inventory sold out between FareConfirm/AddPassenger and Book (a realistic race, same class as §2.2's edge case); malformed/incomplete passenger data (V1) rejected by the supplier."` With: `"Common Errors: stale/expired offer reference; inventory sold out between FareConfirm/AddPassenger and Book (a realistic race, same class as §2.2's edge case) — [Confirmed live 2026-08-23, V1/TravelportNdc] observed as HTTP 409, ErrorCode: NDC-INT-TEC-A0002, ErrorMessage: 'A technical error occurred in NDC integration.', with Context naming the specific exhausted allotment (e.g. infant quota) and confirming the supplier had already allocated an internal hold (a GDS-level PNR) before rejecting ticket issuance — no pnr/bookingToken is returned to the caller in this case; malformed/incomplete passenger data (V1) rejected by the supplier."`
- **§7.14 (near the isdebug credential-exposure finding) — pending** — Replace: `"isdebug: true on Search (§2.1) exposes raw supplierRequest/supplierResponse (observed containing supplier credentials in the SOAP header ...) — INFERRED this is a real, evidenced sensitive-data-exposure risk"` With: `"...same text... [Confirmed live 2026-08-23, V1/TravelportNdc] This is not limited to Search's isdebug flag: Book's error envelope (§2.9), which has no isdebug field at all, was observed returning a raw OriginalSupplierResponse blob unconditionally on a 409 supplier-side rejection. The disclosure risk should be treated as endpoint-error-path-wide, not isdebug-gated."`

## Cleanup / follow-up

- **Needs Void/Cancel (out of reach)** — Supplier-side GDS PNR `HMZQ50` (FLOW-V1-01) — Travelport/QR allocated an internal hold before rejecting Book for infant-quota exhaustion. Our system's Book call errored before returning `pnr`/`bookingToken`, so this cannot be reached via CancelAfterHold/Void — needs checking directly with the GDS/supplier if orphaned holds like this require manual cleanup.
